# MongoDB Environment Variables used by the application

# Connection string (the backend defaults to mongodb://localhost:27017 if not set)
MONGODB_URL=mongodb://localhost:27017

# Database name (the backend defaults to resumematch_pro if not set)
MONGODB_DB=resumematch_pro
